# Security tests

Security coverage will include webhook forgery and replay, project authorization, secret canaries, malicious Dockerfiles, resource exhaustion, forbidden runtime settings, node-agent command replay, registry digest substitution, and cross-project log access.
