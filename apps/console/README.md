# Console

The future developer dashboard for projects, services, environments, deployments, logs, domains, and rollbacks.

Do not scaffold a frontend framework until the Phase 1 workflows and control-plane contract are accepted. The console must consume the versioned API rather than bypassing authorization or writing directly to the system database.
