# Contracts

Versioned API, event, workload, and deployment-state contracts shared across applications.

The OpenAPI source currently lives in [openapi/control-plane.yaml](../../openapi/control-plane.yaml). Generated clients must be reproducible and must not be edited by hand.
