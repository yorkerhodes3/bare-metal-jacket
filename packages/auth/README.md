# Authentication and authorization

Shared identity, organization, project, and role-based authorization policy.

The first release assumes one trusted organization but still enforces explicit project-level authorization so a future tenancy change does not require bypass removal.
