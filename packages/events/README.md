# Events

Definitions for durable deployment events, operational notifications, and immutable audit records.

Events must include a stable identifier, resource identity, actor or system principal, timestamp, correlation identifier, and schema version.
